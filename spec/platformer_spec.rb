require "spec_helper"

describe Platformer do
  it "has a version number" do
    expect(Platformer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
